# Microfinance DB — Group 23

A natural language to SQL chatbot for a microfinance database, built with Gradio and Groq AI.

## Structure

- `notebook/` — main Jupyter notebook
- `data/` — SQLite database
- `diagrams/` — ERD and drawio diagram
- `docs/` — report and progress notes

## Gradio UI

Live demo: *[link here]*

> Link expires in ~1 week, after which the project will be discarded.
